In any programming language, we always learn how to print first, in case of GDScript, the programming language Godot uses, the syntax would be like this:

``` gdscript
extends Node

func _ready():
	print("Hello World!")
```

For now, just know that we need to extend our script from a type of node. We'll learn why later.