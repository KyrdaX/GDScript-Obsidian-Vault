Declaring a variable is, basically, letting know the script that a variable exists, we can do this using the reserved keyword `var`.

``` gdscript
extends Node

func _ready():
	var my_variable
```

This piece of code declares an empty variable called `my_variable`, this doesn't contain any value yet.