In programming, we call a string to a consecutive set of characters, it's the type used when we work with text:

``` gdscript
extends Node

func _ready():
	var my_string = "This is a text!"
```

The variable called `my_string` stores a text, that means its type is String.