To assign a value, we use the `=` symbol, which means assignment:

``` gdscript
extends Node

func _ready():
	var my_variable = "Hello!"
```

This code declares a variable called `my_variable` and assigns the text `Hello!` to it.