Understanding the concept of iteration is really important to understand loops.

We call an iteration one repetition of the code inside a loop. For example, if we had a loop that runs 3 times, we would say that the loop iterates 3 times, meaning that there are 3 iterations.

Each iteration happens one after another until the loop stops. What determines when a loop stops depends on the type of loop and how it is written.