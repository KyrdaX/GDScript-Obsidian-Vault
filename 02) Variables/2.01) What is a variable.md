A variable can be compared to a box, or a bowl of fruits.

It's useful to store all types of data, this makes it actually accessible from the code to use and/or modify its value.

The structure of a variable is as simple as a name (how the variable is accessed) and a value (what the variable actually stores).