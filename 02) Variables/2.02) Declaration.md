Declaring a variable is, basically, letting the script know that a variable exists, we can do this using the keyword `var`.

``` GDScript
extends Node

var my_variable
```

This piece of code declares an empty variable called `my_variable`, this doesn't contain any value yet.